import {styled} from '../theme'

export const StyledNavbarBrand = styled('div', {
  color: '#FFFFFF',
  '& .navbar-brand-title': {
    fontSize: '$4',
    fontWeight: '$semibold',
    color: 'inherit',
    margin: 0,
  },
})
