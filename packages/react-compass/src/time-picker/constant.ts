import {TimePickerFormat} from './types'

export const EMPTY_TIME_PICKER_FORMAT: TimePickerFormat = {
  start: 0,
  end: 0,
  format: '',
  max: 0,
  min: 0,
}

export const EMPTY_DISPLAY_TIME_DROPDOWN_LIST = {
  hour: null,
  minute: null,
  second: null,
  session: null,
}
