import {styled} from '../theme'

export const StyledBCardTitle = styled('div', {
  width: '100%',
  position: 'relative',
  backgroundColor: 'inherit',

  '& .card-default-title': {
    fontSize: '$header4',
    margin: 0,
  },
})
