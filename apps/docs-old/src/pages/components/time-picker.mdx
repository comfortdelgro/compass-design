import {
  Time12HoursPickerControlled,
  Time24HoursPickerControlled,
} from '../../components/TimePickerControlled'
import {TimePicker} from '@comfortdelgro/react-compass'
import {Callout} from 'nextra-theme-docs'

# TimePicker

TimePicker is used to let user select a time or input time.

<Callout>
  You can see how this component is implemented on
  [ladle](https://comfortdelgro.github.io/compass-design/ladle/?story=time-picker--default)
</Callout>

---

### Import:

```js
import {TimePicker} from '@comfortdelgro/react-compass'
```

or

```js
import TimePicker from '@comfortdelgro/react-compass/time-picker'
```

---

### Basic Example

```tsx {22}
function BasicExample() {
  return <TimePicker defaultValue={'12:00 PM'} />
}
```

<TimePicker defaultValue={'12:00 PM'} />

---

### Disable

```tsx {22}
function TimePickerDisable() {
  return <TimePicker defaultValue={'12:00 PM'} isDisabled />
}
```

<TimePicker defaultValue={'12:00 PM'} isDisabled />

---

### Readonly

```tsx {22}
function TimePickerReadonly() {
  return <TimePicker defaultValue={'12:00 PM'} isReadOnly />
}
```

<TimePicker defaultValue={'12:00 PM'} isReadOnly />

---

### With 12-hours uncontrolled

```tsx {22}
function Time12HoursPickerUncontrolled() {
  return (
    <TimePicker
      formatTime='hh:mm:ss AA'
      views={['hours12', 'minutes', 'seconds', 'sessions']}
      defaultValue={'11:05:50 PM'}
    />
  )
}
```

<TimePicker
  formatTime='hh:mm:ss AA'
  views={['hours12', 'minutes', 'seconds', 'sessions']}
  defaultValue={'11:05:50 PM'}
/>
---

### With 12-hours controlled

```tsx {22}
function Time12HoursPickerControlled() {
  const [value, setValue] = useState('')
  return (
    <TimePicker
      formatTime='hh:mm:ss AA'
      views={['hours12', 'minutes', 'seconds', 'sessions']}
      value={value}
      onTimeChange={(value: string) => {
        console.log('Value 12-hours changed: ', value)
        setValue(value)
      }}
    />
  )
}
```

<Time12HoursPickerControlled />

---

### With 24-hours uncontrolled

```tsx {22}
function Time24HoursPickerUncontrolled() {
  return (
    <TimePicker
      formatTime='HH:mm:ss'
      views={['hours24', 'minutes', 'seconds']}
      hasFooter
      defaultValue={'05:05:55'}
    />
  )
}
```

<TimePicker
  formatTime='HH:mm:ss'
  views={['hours24', 'minutes', 'seconds']}
  hasFooter
  defaultValue={'05:05:55'}
/>

---

### With 24-hours controlled

```tsx {22}
function Time12HoursPickerControlled() {
  const [value24H, setValue24H] = useState('')
  return (
    <TimePicker
      formatTime='HH:mm:ss'
      views={['hours24', 'minutes', 'seconds']}
      hasFooter
      value={value24H}
      onTimeChange={(value: string) => {
        console.log('Value 24-hours changed: ', value24H)
        setValue24H(value)
      }}
    />
  )
}
```

<Time24HoursPickerControlled />

## Props

### TimePicker

| Name         | Type         | Default                              | Description                                                                             |
| :----------- | :----------- | :----------------------------------- | :-------------------------------------------------------------------------------------- |
| value        | `string`     | -                                    | The selected value. Used when the component is controlled.                              |
| defaultValue | `string`     | -                                    | The default value. Used when the component is not controlled.                           |
| hourStep     | `number`     | `1`                                  | Step over hours.                                                                        |
| minuteStep   | `number`     | `1`                                  | Step over minutes.                                                                      |
| hasFooter    | `boolean`    | `True`                               | If true, displays the footer with OK button.                                            |
| css          | `CSS`        | —                                    | The system prop that allows defining system overrides as well as additional CSS styles. |
| formatTime   | `string`     | `hh:mm AA`                           | Format of the date when rendered in the input(s).                                       |
| views        | `ViewType[]` | `['hours24', 'minutes', 'sessions']` | Available views.                                                                        |
| onTimeChange | `Function`   | -                                    | Callback fired when the value changes.                                                  |
