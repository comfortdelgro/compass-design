import {Callout} from 'nextra-theme-docs'
import styles from '../../styles/Home.module.css'
import SpeedDial from '@comfortdelgro/react-compass/speed-dial'

# SpeedDial

Provides the behavior and accessibility implementation for a speed-dial component.

---

### Import:

```js
import {SpeedDial} from '@comfortdelgro/react-compass'
```

or

```js
import SpeedDial from '@comfortdelgro/react-compass/slider'
```

---

### Basic Usage:

<section className={styles.avatarShowcaseContainer} style={{display: 'flex', flexDirection: 'column', gap: '2rem' }}>
 <div>
      {/* Render other content */}
      {/* Render the SpeedDial component with the actions prop */}
      <h1>Default:</h1>
      <SpeedDial actions={[
    {
      name: 'Action 1',
      icon: '+',
      onClick: () => console.log('Action 1 clicked'),
    },
    {
      name: 'Action 2',
      icon: '-',
      onClick: () => console.log('Action 2 clicked'),
    },
    // Add more actions as needed
  ]} position='up' />
    </div>
</section>

```js
<SpeedDial actions={[
    {
      name: 'Action 1',
      icon: '+',
      onClick: () => console.log('Action 1 clicked'),
    },
    {
      name: 'Action 2',
      icon: '-',
      onClick: () => console.log('Action 2 clicked'),
    },
    // Add more actions as needed
  ]} position='up' />
```

---

### Props:

| Name               | Type                       | Default     | Description                                                                                                |
| :----------------- | :---------------------     | :---------- | :--------------------------------------------------------------------------------------------------------- |
| actions            | `Array<Object>`            | `{}`        | Array of Object define the actions can show on speed dial.                                                 |
| position           | `string`                   | `up`        | Define the position where the actions list show                                                            |

---

<Callout>
  Check out our [ladle](/ladle?story=speed-dial) for more info on this
  component.
</Callout>
