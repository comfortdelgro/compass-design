<Pagination
    rowsOptions={[5, 10, 15, 20]}
    rowsPerPage={5}
    onRowsPerPageChange={(rows) => {
        setRows(rows)
    }}
    page={page}
    total={10}
    onChange={(page) => setPage(page)}
    count={100}
/>
