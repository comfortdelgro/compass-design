import { GutterSide } from '../gutter';
interface IconProps {
    side?: `${GutterSide}`;
    isExpand: boolean;
}
declare const Icon: ({ side, isExpand }: IconProps) => import('../../../../../node_modules/.pnpm/react@18.3.1/node_modules/react/jsx-runtime').JSX.Element;
export default Icon;
